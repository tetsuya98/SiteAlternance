{% trans_default_domain 'FOSUserBundle' %}

<form id="reset_form" action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">
    <div>
        <label for="username">{{ 'resetting.request.username'|trans }} : </label>
        <input type="text" id="username" name="username" required="required" />
    </div>
    <div>
        <input class="class_button" type="submit" value="{{ 'resetting.request.submit'|trans }}" />
    </div>
</form>

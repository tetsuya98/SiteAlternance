{% extends 'base.html.twig' %}
{% block title %} {{ parent() }} {% endblock %}
{% block body %}

    <div class="col-md-6 col-12 mr-md-3 form-group">
        <div class="side-container">
            <div class="head">
                <h2>Offre</h2>
            </div>
            <div class="body px-3">
                <div class="col-12 d-flex justify-content-between">
                    <div class="row">
                        <div class="img-icon">
                            {% if offre.imageName %}
                                <img src="{{ asset('offres/avatar/' ~ offre.imageName)}}">
                            {% else %}
                                <img src="{{ asset('assets/img/folder.jpg') }}">
                            {% endif %}
                        </div>
                        <div class="ml-2">
                            <h4>{{ offre.titre }}</h4>
                            <p>{{ offre.typeContrat.titre }} ({{ offre.nbSemaine }} semaines)</p>
                            <p>Publié le {{ offre.crDate|date("d/m", "Europe/Paris") }}</p>
                        </div>
                    </div>
                    <div class="row">
                        <div>
                            {#<h5>{{ offre.user.userEntreprise.nomUtilisateur }}</h5>#}
                            <h5>{{ offre.user.username }}</h5>
                        </div>
                        <div class="img-icon ml-2">
                            {% if offre.user.imageName %}
                                <img src="{{ asset('images/Avatar/' ~ offre.user.imageName) }}">
                            {% else %}
                                <img src="{{ asset('assets/img/folder.jpg') }}">
                            {% endif %}
                        </div>
                    </div>
                </div>
                <h3>Description</h3>
                <p>{{ offre.description }}</p>
                <h3>Compétences</h3>
                <div class="d-flex">
                    {% for competence in offre.competences %}
                        <button class="btn btn-custom mr-1">{{ competence.competences }}</button>
                    {% endfor %}
                </div>
                <h3>Pièce jointe</h3>
                {% if offre.fileName %}
                    <a class="btn btn-custom" href="{{ asset('offres/file/' ~ offre.fileName) }}">Télécharger</a>
                    <p>{{ offre.fileName }}</p>
                {% else %}
                    <p>Pas de pièce jointe</p>
                {% endif %}

                <h3>Candidature</h3>
                {% if candidature %}
                    <p>{{ candidature.status }}</p>
                {% else %}
                    <a href="{{ path('offres_postule', {'offre' : offre.id}) }}" class="btn-custom mb-2">Candidater</a>
                {% endif %}
            </div>
        </div>
    </div>

    <div class="col-md-6 col-12 ml-md-3">
        <div class="side-container">
            <div class="head">
                <h3>Nos conseils</h3>
            </div>
            <div class="body mt-0 px-0 pt-0">
                <div class="w-100">
                    <img src="{{ asset('assets/img/advices/1.jpg') }}" alt="Conseils" class="img-fluid"/>
                </div>
                <div class="p-4 text-justify">
                    <h3>Bien postuler à une offre</h3>
                    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Alias consequuntur corporis hic
                        laboriosam minus mollitia nisi, officiis pariatur perferendis praesentium quas,
                        tempora vero voluptatum. Architecto enim magni nihil provident tempora.</p>
                    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Alias consequuntur corporis hic
                        laboriosam minus mollitia nisi, officiis pariatur perferendis praesentium quas,
                        tempora vero voluptatum. Architecto enim magni nihil provident tempora.</p>
                </div>
            </div>
        </div>
    </div>

{% endblock %}